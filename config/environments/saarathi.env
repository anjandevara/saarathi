# Settings for testing the Saarathi application (the command center) on self.
# Start the Saarathi dev server first, then run: TEST_ENV=saarathi npx playwright test tests/saarathi
ENV_NAME=saarathi
BASE_URL=http://localhost:3000
