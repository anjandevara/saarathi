# Settings for running tests against Production.
# Change BASE_URL to your team's real production URL.
# Be careful what tests you allow to run here (read-only / smoke tests only, normally).
ENV_NAME=prod
BASE_URL=https://www.example.com
